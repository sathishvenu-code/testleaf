let inputsentence = "Hello world";

let splitWords = inputsentence.split(" ");

console.log(`last word in the sentense is ${splitWords[splitWords.length-1]}`);

console.log(`length of last word in the sentense is ${splitWords[splitWords.length-1].length}`);

